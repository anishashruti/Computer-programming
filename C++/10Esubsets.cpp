#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    int arr[50];
    int subs[20][20];
    int size;
    cin >> size;
    int j=0, k=0;
    for (int i=0;i<size;i++){
        cin >> arr[i];      
    }
    for (int i = 0; i < k;i++)
    {

    }

        return 0;
}