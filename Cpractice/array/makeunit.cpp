#include <iostream>
using namespace std;

int main() {    
    int numbers[25],size;
    cin >> size;
    for (int i = 0; i < size; ++i) {
        cin >> numbers[i];
    }
    for (int i = 0; i < size; ++i)
    {
        if(numbers[i]> 0)
        {

        }

    }

    return 0;
}